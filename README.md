
# Compare Everything! — Next.js Starter (Generated)

This is a generated starter for the Compare Everything! project (Next.js + Tailwind + NextAuth + Firestore admin API).
Files were assembled by ChatGPT — update environment variables before running.

## Quick start

1. Copy `.env.local.template` to `.env.local` and fill values.
2. Install deps: `npm install`
3. Run dev: `npm run dev`

## Notes

- Admin users must be listed in `ADMIN_USERS` env var (comma-separated).
- This project uses firebase-admin on server routes; do not leak service account to the client.
